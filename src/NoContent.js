import React from "react";

const NoContent = () => {
  return (
    <div className="flex items-center justify-center text-2xl text-gray-500 font-medium h-[77vh]">
      Select a note, or create a new one.
    </div>
  );
};

export default NoContent;
